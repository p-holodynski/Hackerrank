# Day 5 : Loops
```javascript
function main() {
    const n = parseInt(readLine(), 10);
    for (var i = 1; i < 11; i++){
        var result = n * i;
        console.log(n + " x " + i + " = " + result);
        }
}
```
